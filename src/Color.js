import React from "react";

const AppTheme = {
    light:{
        textColor: "#000",
        backgroundColor: "#fff"
    },
    dark: {
        textColor: "#fff",
        backgroundColor: "#000"

    }
}

export default AppTheme;